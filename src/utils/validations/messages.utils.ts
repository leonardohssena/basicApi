const UNIQUE = 'Value already exists'

const REQUIRED = 'Value required'

const INVALID = 'Value invalid'

const MINLENGTH = (min) => `Mininum of ${min} caracters`

const MAXLENGTH = (max) => `Max of ${max} caracters`

export { REQUIRED, UNIQUE, INVALID, MINLENGTH, MAXLENGTH }
